require 'spec_helper'

describe Lita::Substitution do
  it 'has a version number' do
    expect(Lita::Substitution::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
